# Landing Page Tailwind (TechPerkasa)

Halaman statis landing page sederhana dibuat dengan HTML dan Tailwind CSS (menggunakan CDN).

File utama:
- `index.html` — Landing page responsive dengan header/navigation, hero, fitur, about, contact form, dan footer.

Menjalankan secara lokal:
1. Buka `d:/Documents/smt5/TA3_PPW/index.html` di browser (double-click atau klik kanan -> Open with > browser).
2. Halaman menggunakan Tailwind CDN, tidak memerlukan build step.

Catatan:
- Form kontak memiliki handling klien sederhana (tidak mengirim data ke server). Untuk integrasi backend, tambahkan endpoint dan kirim data via fetch.
- Jika mau gunakan Tailwind versi custom atau build, konversi ke proyek dengan npm/postcss dan ikuti dokumentasi Tailwind.
